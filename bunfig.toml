[serve.static]
plugins = ["bun-plugin-tailwind"]
env = "BUN_PUBLIC_*"

[install]
# Don't auto-install peer deps: bun-plugin-tailwind peers on the npm `bun` package,
# whose shim in node_modules/.bin would shadow the real bun binary in package scripts.
peer = false
